import Store from 'herculex';
import getRightsList from '../../services/rightsList';

export default new Store({
  connectGlobal: true, // 是否关联global
  state: {
  },
  mutations: {
  },
  actions: {
  }
});
